# Root services: Streamlit UI, ingestion, mock API clients, and DB readers.
# Copy to .env for local use. Never commit the populated .env file.

APP_ENV=development

# Local service ports.
FRONTEND_PORT=8501
SAP_API_PORT=8001
AGENT_API_PORT=8000

# Local SQLite databases (relative paths only).
DATABASE_URL=sqlite:///./agent_app/ap_agent.db
MASTER_DATABASE_URL=sqlite:///./data/master/ap_master.db

# AWS/RDS alternative. Inject real credentials with AWS Secrets Manager or SSM.
# APP_ENV=aws
# DATABASE_URL=postgresql+psycopg://agent_user:REPLACE_ME@db.example.internal:5432/ap_automation?sslmode=require
# MASTER_DATABASE_URL=postgresql+psycopg://master_user:REPLACE_ME@db.example.internal:5432/ap_automation?sslmode=require

# Local service endpoints.
MOCK_API_BASE_URL=http://127.0.0.1:8001
AP_AGENT_BASE_URL=http://127.0.0.1:8000
SAP_BASE_URL=http://127.0.0.1:8001
KEFRON_BASE_URL=http://127.0.0.1:8001
POSTED_INVOICE_API_BASE_URL=http://127.0.0.1:8001

# Replace these placeholders with local mock credentials or injected secrets.
SAP_USERNAME=REPLACE_ME
SAP_PASSWORD=REPLACE_ME
KEFRON_API_KEY=REPLACE_ME
GROQ_API_KEY=REPLACE_ME

UNSTRUCTURED_INPUT_DIR=./unstructured_ingestion/unstructured_inputs

# Storage configuration. Steps 15A-15C added local/S3 artifact storage.
# Use local backend for development; use s3 backend on AWS/EC2.
STORAGE_BACKEND=local
S3_BUCKET_NAME=
S3_REGION=
AWS_REGION=
# Root prefix only; do not use one flat folder for invoice artifacts.
S3_PREFIX=ap-demo/
S3_ENDPOINT_URL=
# AWS_PROFILE is optional for local AWS testing. EC2 should use an IAM role.
AWS_PROFILE=

# LOCAL SQLITE DEMO ONLY. Keep false/omitted for PostgreSQL/RDS.
ALLOW_DESTRUCTIVE_MASTER_RESET=true
# ALLOW_DESTRUCTIVE_MASTER_RESET=false
